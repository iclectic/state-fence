import 'package:state_fence_flutter/state_fence_flutter.dart'; // ignore: unused_import
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('state_fence_flutter resolves and loads', () {
    // The package is intentionally empty in Phase 1. This test proves that
    // the library resolves and the test runner can be invoked.
    expect(true, isTrue);
  });
}
