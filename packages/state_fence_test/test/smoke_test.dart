import 'package:state_fence_test/state_fence_test.dart'; // ignore: unused_import
import 'package:test/test.dart';

void main() {
  test('state_fence_test resolves and loads', () {
    // The package is intentionally empty in Phase 1. This test proves that
    // the library resolves and the test runner can be invoked.
    expect(true, isTrue);
  });
}
